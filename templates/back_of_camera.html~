{% extends "layout.html" %}

{% block content %}
<div class="container">
  <div class="container text-center py-5">
    <h1 class="mb-4">Back of the Camera</h1>
    <p>Choose a feature to learn about:</p>

    <div class="row justify-content-center">
      <div class="position-relative" style="max-width: 800px;"> <!-- container with relative position -->
    
        <!-- Camera Image -->
        <img src="../static/images/back_camera.png" alt="Camera" class="img-fluid w-100 camera-img" />
    
        <!-- Star Button -->
        <button class="star-btn position-absolute" style="top: 30%; left: 10%;" onclick="showDescription('Menu')">⭐</button>
    
        <!-- Description Card -->
        <div id="Menu" class="desc-card d-none">
          <button class="close-btn" onclick="closeDescription('Menu')">×</button>
          <p>If you click on the menu, you will be able to change many settings. This is where you change the ISO.</p>
        </div>
    
      </div>
    </div>
    

    <h1 class="mt-5">As I mentioned earlier, ISO, aperture, and shutter speed are the three core settings that control camera exposure. Let's learn about aperture and ISO:</h1>
    <div class="d-flex justify-content-center gap-4 mt-4">
      <a href="{{ url_for('stacey_aperture') }}" class="feature-btn aperture-btn">Aperture</a>
      <a href="{{ url_for('stacey_iso') }}" class="feature-btn iso-btn">ISO</a>
    </div>

    <div class="text-center my-4">
      <a href="{{ url_for('camera') }}" class="back-btn">
        ← Front of Camera 
      </a>
    </div>

  </div>
</div>

<!-- JavaScript for Showing and Hiding Description -->
<script>
  function showDescription(id) {
    document.querySelectorAll('.desc-card').forEach(el => el.classList.add('d-none'));
    document.getElementById(id).classList.remove('d-none');
  }

  function closeDescription(id) {
    document.getElementById(id).classList.add('d-none');
  }
</script>
{% endblock %}

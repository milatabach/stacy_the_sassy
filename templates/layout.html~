<!doctype html>
<html lang="en">
  <head>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="../static/main.css" />
  </head>

  <body>
    <header class="site-banner d-flex justify-content-between align-items-center px-4 py-2">
      <a href="{{ url_for('home') }}" class="banner-link">How to Use a Camera</a>

      <!-- Dropdown Menu -->
      <div class="dropdown">
        <button class="nav-btn btn btn-outline-dark dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
          Navigate
        </button>

        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
          <!-- Home -->
          <li><a class="dropdown-item" href="{{ url_for('home') }}">Home</a></li>

          <!-- Lesson Dropdown -->
          <li><a class="dropdown-item" href="{{ url_for('learning') }}">Lesson</a></li>
          <li><a class="dropdown-item lesson-section" href="{{ url_for('camera') }}">Camera Front</a></li>
          <li><a class="dropdown-item lesson-section" href="{{ url_for('back_of_camera') }}">Back of Camera</a></li>
          <li><a class="dropdown-item back-camera-section" href="{{ url_for('aperture_opening') }}">Aperture</a></li>
          <li><a class="dropdown-item back-camera-section" href="{{ url_for('iso_opening') }}">Iso</a></li>
          <li><a class="dropdown-item back-camera-section" href="{{ url_for('shutter_opening') }}">Shutter Speed</a></li>
          <!-- Quiz -->
          <li><a class="dropdown-item" href="{{ url_for('quiz') }}">Quiz</a></li>
        </ul>
      </div>
    </header>

    <div class="container mt-4">
        {% block content %}
        {% endblock %}
    </div>
  </body>
</html>
